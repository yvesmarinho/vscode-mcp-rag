# Qdrant configuration for MCP RAG server
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=  # Leave empty for local; set for remote/cloud

# Collection name in Qdrant
QDRANT_COLLECTION=project_docs

# Embeddings provider (fastembed | sentence-transformers | openai)
# For CPU-only: use fastembed or sentence-transformers
EMBEDDINGS_PROVIDER=fastembed

# Model name (depends on provider)
# FastEmbed: BAAI/bge-small-en-v1.5, BAAI/bge-base-en-v1.5, etc.
# SentenceTransformers: all-MiniLM-L6-v2, all-mpnet-base-v2, etc.
# OpenAI: (auto, no need to set)
MODEL_NAME=BAAI/bge-small-en-v1.5

# OpenAI (only if EMBEDDINGS_PROVIDER=openai)
# OPENAI_API_KEY=sk-...
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
