# Docker Environment Configuration
# Copy this to docker/.env and customize for your setup
# This file is optional - leave blank for defaults

# Optional: Enable API key authentication for Qdrant
# If set, all API calls must include: -H "api-key: value"
# Leave empty (or omit) for local development without authentication
QDRANT_API_KEY=

# Optional: Set project name (default: docker)
# Used as prefix for container names: ${COMPOSE_PROJECT_NAME}_qdrant_1
# COMPOSE_PROJECT_NAME=myproject

# Optional: Set custom image tag (default: latest)
# To use specific version: qdrant/qdrant:v1.7.0
# QDRANT_IMAGE=qdrant/qdrant:v1.7.0
